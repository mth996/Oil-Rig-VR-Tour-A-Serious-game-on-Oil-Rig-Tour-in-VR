using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class RobotNavmeshController : MonoBehaviour
{
    /*[SerializeField] 
    public Transform movePositionTransform;
    public Transform guidehub;
    public Transform locationA;
    public Transform locationB;
    public Transform locationC;
    private NavMeshAgent navMeshAgent;
    //bool isinGuideHub = true;
    //bool isinLocationA = false;
    //bool isinLocationB = false;
    //bool isinLocationC = false;
    //bool isinLocationA = false;
    
    private void Awake () 
    {
        navMeshAgent = GetComponent<NavMeshAgent>();
        navMeshAgent.destination = guidehub.position;
    }
    private void OnTriggerEnter(Collider collision)
    {
        Debug.Log(collision);
        
        if (collision.tag == "Player")
        {   
            navMeshAgent.destination = movePositionTransform.position;
        }
    }*/
}
